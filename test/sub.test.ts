import { sub } from "../src/sub"

test( "bbbbbbbbbbbbbbbb", () => {
    const rs = sub(5, 2)
    expect(rs).toBe(3)
})

test( "bbbbbbbbbbbbbbbb", () => {
    const rs = sub(20, 10)
    expect(rs).toBe(10)
})

test( "bbbbbbbbbbbbbbbb", () => {
    const rs = sub(7,2)
    expect(rs).toBe(5)
})

